# 譜台 改進計畫

針對現行 `piano-trainer.html`（單檔 1697 行 / VexFlow 4.2.3 CDN）的六項需求：
雙手同步、四小節連續生成、回顧樂譜、全調性、和弦豐富化、iPad 可用。

---

## 一、現況診斷（問題出在哪一行）

| 需求 | 現在的實作 | 為什麼不夠 |
|---|---|---|
| 雙手 | `bassLine()` L843–890 | 左手只有三種寫法：lv≤2 全音符持續低音、lv≤4 二分音符、lv≥5 固定 `[0,4,7,4]` 阿爾貝提。永遠是「右手旋律＋左手伴奏」，沒有真正的手部獨立，也沒有休止、沒有對位、沒有齊奏。而且 `clef` 預設 `treble`，雙手要手動選「大譜表」。 |
| 四小節連續 | `onBarPassed()` L1584 → `generate()` | 到第 N 小節整張譜被 `scoreEl.innerHTML=""` 清掉重畫。沒有預讀（視譜最重要的就是眼睛先跑），沒有進行中的游標，段與段之間也毫無銜接（每四小節都是一座孤島，永遠從主和弦重新開始）。 |
| 回顧樂譜 | 無 | 全域用 `Math.random()`，題目一換就永遠消失，無法重現、無法比對、無法列印成練習單。 |
| 全調性 | `KEYSIG` L558 / `VEXKEYSIG` L563 | 只有 9 個大調（C G D A E F B♭ E♭ A♭）。缺 B / F♯ / C♯ / D♭ / G♭ / C♭，**完全沒有小調**。`accForKey()` 對未知調直接回傳 0，是靜默錯音。旋律引擎 `degreeMap`/`chordDegrees` 也硬寫大調三度堆疊。 |
| 和弦太單調 | `progression()` L922 / `CHORDS` L493 / `voicing()` L530 | 只有 5 種進行、6 種和弦、4 種 voicing，而且**每個和弦都畫成一顆全音符、只在低音譜號**。單調的真正來源不是進行少，是「一小節一顆全音符、右手完全沒事做」。 |
| iPad | `@media (max-width:900px)` L196 | 只是把 322px 側欄疊到譜的上方 → iPad 直立時要先滑過一整頁設定才看得到譜，當譜架完全不能用。另外：無 PWA、無離線、按鈕觸控區太小、iOS 靜音鍵會讓節拍器整個沒聲、螢幕會自己暗掉。 |

另外三個會擋住後續開發的技術債：

- `makeVoice(...).setStrict(false)`（L1092）把「節奏總長不等於一小節」的錯誤靜靜吞掉。節奏一變複雜就會出現排版鬼畫符。
- `VF.Beam.generateBeams(topNotes)`（L1121）沒給 `groups`，4/4 的八分音符會連成一整條，不照拍分組，視譜時很難讀。
- `Metro` 用 `setInterval(25ms)` 排程、視覺回呼用 `setTimeout`（L1362–1386）。iOS 背景 / 螢幕變暗時 timer 會被節流，節拍會漂。

---

## 二、階段規劃

每一階段結束都是一個能直接用的檔案，不會做到一半不能練。

### P0 — 地基（其他五項都靠它）✅ 已完成

> 交付：`index.html` + `css/app.css` + `js/`（12 個模組）。舊的 `piano-trainer.html` 原封不動保留當備份。
> 決策紀錄：交付走 GitHub Pages + PWA（使用者已有 GitHub 與 dipvinyl.tw 的線上系統）；拆多檔模組；順序 P0→P5。


1. **種子亂數**：`mulberry32(seed)` 取代所有 `Math.random()`。每一段練習 = `{seed, cfg}`，可重現、可回顧、可分享、可列印。這是「回顧樂譜」的前提。
2. **調性核心重寫**：拿掉 `KEYSIG` 查表，改成 `Key(tonic, mode)` 物件
   - 15 大調：C G D A E B F♯ C♯ / F B♭ E♭ A♭ D♭ G♭ C♭
   - 15 小調 × 自然 / 和聲 / 旋律（升 7 音的 F♯♯ 這種重升記號要正確處理）
   - 級數 → 和弦品質改成查 mode 表（大調 I ii iii IV V vi vii°；和聲小調 i ii° III+ iv V VI vii°）
   - `VEXKEYSIG` 補滿 30 個（VexFlow 4 原生支援 `"C#"`、`"Abm"` 這類寫法）
3. **節奏正確性**：`setStrict(true)`，出題時斷言每小節時值總和 = 拍數；beaming 依拍號分組（4/4 每拍一組、3/4 每拍一組、6/8 每三個八分一組）。

> 產出：畫面看起來一樣，但每個調都能出題、每題都能重現。
>
> **實測結果**：30 個調 × 6 難度 × 3 拍號 × 3 譜號 = 810 次繪製，零錯誤、零空白、
> strict voice 零降級（代表每一小節時值都通過 VexFlow 嚴格驗證）。
> 音階拼寫逐調驗證：15 大調全為 2212221、15 小調全為 2122122，含 C♭ 大調與 a♯ 小調。
> 小調導音正確寫出重升記號（g♯ 小調 → F𝄪、a♯ 小調 → G𝄪）。

**過程中抓到並修掉的實際 bug**

- 節奏驗證器一上線就抓到我自己寫錯的 2/4 節奏庫（誤用 3/4 的型，14 個型全部少一拍）——
  這正是舊版 `setStrict(false)` 會靜靜吞掉的那類錯誤
- 小節交界處用「當前小節的變化音」回推前一個音，導致小調增二度迴避在跨小節時失效（26/600 漏網）。
  改成追蹤真正寫下的音，並把扣分改成硬排除；躲不掉時退回旋律小調的寫法（把前面的 ♭6 一起升上去），
  而不是硬吞那個刺耳的音程
- 舊版 `onBarPassed` 在第 N 小節的**第一拍**就換題，「每 4 小節」實際只給了 3 小節。
  節拍器改為回報「已彈完幾小節」

### A — 旋律與和弦（追加需求）✅ 已完成

使用者追加要求：旋律與和弦變化多一點、旋律要走在和弦進行上、彈起來不能乾澀。

**A-1 語料庫**：`tools/build-harmony-stats.mjs` 從
[When-in-Rome](https://github.com/MarkGotham/When-in-Rome) 功能和聲語料庫（分析部分 CC BY-SA 4.0）
萃取 **921 份分析、3,597 個調性段落、49,785 個和弦** 的接續統計 → `js/data/harmony-stats.js`（30 KB）。
含 Bach / Mozart / Schubert / Fanny Mendelssohn / Clara Schumann / Monteverdi，
以及 Reger、Rimsky-Korsakov、Tchaikovsky、Kostka 的和聲學教科書習題。
工具只下載、只計數、只輸出頻率表，原始分析檔不落地、不進版控。
（另評估過 Essen Folksong Collection，其 CCARH 授權明文限制散布，未採用。）

**A-2 和聲產生器**：`js/core/roman.js`（羅馬數字 → 實際音，含轉位、七和弦、應用和弦、拿波里六）
＋重寫 `js/gen/harmony.js`。演算法是「可達性回推 + 語料庫加權前向取樣」：
先抽終止式 → 從終點回推每個位置走得到終點的和弦集合 → 再從頭加權取樣。
既走真實存在的接續，又保證收得漂亮。

**A-3 旋律引擎**：改成傳統的「骨架 + 加花」兩層。
骨架管和弦音、七音下行解決、導音上行解決、輪廓弧線；
加花層不再查表列舉音型，而是先決定「這幾步各走幾度」再打散順序 ——
位移總和固定所以一定走到目標音，每步都不為零所以結構上不可能產生重複音。

| 指標（30 調 × 4 難度 × 12 seed = 1,440 題） | 改版前 | 改版後 |
|---|---|---|
| 強拍落在和弦音 | — | **97.7%** |
| 非和弦音「級進進入且級進離開」（真正的經過音／鄰音） | 27.7% | **69.1%** |
| 七音級進下行解決 | 38.7% | **72.5%** |
| 重複同音佔比（真實旋律約 5–10%） | 23.6% | **1.3%** |
| 級進佔全部音程（調性旋律約 6–8 成） | 56.6% | **74.8%** |
| 四小節和聲進行的變化量（難度 5） | 13 個寫死模板 | **247 種** |

過程中修掉的實際 bug：轉位會把和弦音陣列旋轉，導致七音解決與導音解決一直找錯音；
加花層踩到七音卻不解決；樂句從 V 開頭；`V7 → V` 這種倒行（語料庫記譜造成的假接續）；
`iii/III` 這類不是應用和弦的暫時主音化標記。

### P1 — 雙手同時練習 ✅ 已完成

1. **左手模式庫** `js/gen/bass.js` —— 8 種寫法，依難度與拍號自動開放：

   | 模式 | 最低難度 | 備註 |
   |---|---|---|
   | 持續低音 | 1 | |
   | 根音－五音 | 2 | |
   | 塊狀和弦 | 3 | |
   | 低音－和弦－和弦 | 3 | 只在 3/4 出現 |
   | 分解和弦 | 4 | 上行／下行隨機 |
   | 阿爾貝提低音 | 4 | 只在 4/4 出現 |
   | 與右手平行三／六度 | 5 | 兩手同節奏齊奏 |
   | 與右手反向對位 | 5 | 右手上行時左手下行 |

2. **角色互換**：`hands: "swap"` —— 左手旋律、右手和弦。
3. **練習手設定**：雙手 / 雙手（左手旋律）/ 只有右手 / 只有左手，預設雙手。「譜號」選單已取消。
4. 難度曲線目前綁在「哪些左手模式可用」上。更細的兩手節奏交錯（左手比右手慢一倍、切分交錯）尚未做。
5. 指法提示：未做。

**實測**：30 調 × 5 難度 × 3 拍號 × 4 種練習手 = 1,800 次繪製，零空白、零錯誤、strict voice 零降級。

**已知殘留**：兩手之間的平行五度／八度沒有主動迴避。實測 15,661 次兩手同時移動中
平行五度 0.44%、平行八度 1.89%（合計 2.3%）；「平行三／六度」模式為 0%。
在伴奏音型裡這個比例不構成問題（嚴格禁止平行是四部合唱體的規則），但它不是零。

### P2 — 四小節連續流 ✅ 已完成

把「產生 → 清空 → 重畫」改成串流：

- **段落佇列**：一段 = 4 小節（可設 2/4/8）。永遠維持 `[正在彈, 下一段(已畫好), 再下一段(背景生成)]`。
- **雙行顯示**：上行是正在彈的，下行是**已經畫好的下一段** → 眼睛可以提前跑，這是視譜訓練的核心，現在完全沒有。
- **進行中游標**：跟著 `Metro` 走的半透明色塊掃過當前小節（用 AudioContext 時間 + `requestAnimationFrame`，不用 setTimeout，才不會漂）。
- **段落銜接**：下一段的開頭要接得住上一段的結尾——
  - 上一段收在 V（半終止）→ 下一段從 I 起
  - 帶著上一段最後一個音的音域進去，不會突然跳兩個八度
  - 可選「每段換調」（五度圈輪替 / 隨機），把全調性練習變成不間斷的流
- **換段時機**：下一段一直都在螢幕上，換段只是把它「升上來」。

**實作** `js/stream.js` + `js/main.js`

- 兩個實體列用 CSS `order` 對調上下。換段時**完全不重畫**已經在看的譜，
  只把剛空出來的那一列拿去畫新的下一段。實測連跑 30 段，
  升上來的 SVG 與換段前「下一段」的 SVG 位元組完全相同 —— 30/30 無縫。
- 每一列各自保存自己的播放計畫，換段時直接沿用，不必重算。
- 游標吃 `Metro.position()`（AudioContext 硬體時鐘）。requestAnimationFrame 只負責補平順；
  分頁不可見時 rAF 會被瀏覽器停掉，所以節拍器每一拍也主動推一次，游標不會凍住。
  實測游標框與小節譜表對齊到 0.2px 內（SVG 的 CSS 縮放有換算），預備拍期間隱藏。
- 段落銜接實測：交界處旋律平均只跳 1.7–2.0 個音級，24 次交界零次超過八度；
  半終止／假終止之後，下一段必定從原位主和弦解決回來；五度圈輪替完整走一圈回到起點。
- 流程三選一：連續流（雙行預讀）/ 段落結束換題 / 手動換題。原本的「自動換題」選單已取代。

**過程中修掉的 bug**：`replay()`（重來同一題）只重用 seed，卻丟掉五度圈進度與
段落銜接的起始音 —— 在輪替模式下連調都會跑掉。改成把實際用過的整份 cfg 存下來重跑。
（這也正是 P4 回顧需要存的東西：一段練習幾十 bytes。）

### P3 — iPad 化與部署 ✅ 已完成

實測（Chromium，四種視窗尺寸）：390×844 手機、820×1180 iPad 直立、
1180×820 iPad 橫向、1440×900 桌機 —— 全部零橫向溢出、頂欄不換行。

- **版面**：固定頂欄（漢堡 · 拍點條 · 開始 · 換一題）＋ 設定側滑抽屜。
  抽屜只在「寬螢幕**且有滑鼠**」時釘住 —— iPad 橫向雖然也有 1180px，
  但它是譜架，那 322px 要留給五線譜。
- **每行小節數**改成看實際可用寬度：大譜表 <560px 一小節、<860px 兩小節、否則四小節。
  820 直立實測排成 2 行 × 2 小節；390 手機排成 4 行 × 1 小節。
- **譜面縮放** 80–200%，用 `ctx.scale` 整張放大而不是把小節擠在一起。
  實測符頭 9.5px → 23.9px，游標在 150% 時對齊誤差 0px。
- **觸控**：`pointer:coarse` 時所有目標 ≥44px、select 字級 16px（低於此 iOS 會自動放大頁面）、
  `touch-action:manipulation` 關掉雙擊縮放、safe-area 內縮。
- **手勢**：左滑換題、右滑開抽屜、點譜面播放。負向案例也測了 ——
  垂直捲動與慢速拖曳都不會誤觸。
- **iOS 音訊**：首次手勢就把 AudioContext 叫起來；`navigator.audioSession` 宣告成
  playback 以繞過實體靜音鍵，不支援時退回無聲迴圈（測試環境走的正是後者）。
- **螢幕常亮**：Wake Lock，並在分頁重新可見時重新取得（系統會在背景時自動釋放）。
- **PWA**：manifest、四種尺寸圖示（`tools/make-icons.mjs` 純 zlib 手工產生 PNG，無外部相依）、
  apple-touch-icon、standalone 全螢幕。
- **離線**：Service Worker 快取 28 個檔案含 `vendor/vexflow.js`（0.95 MB）。
  **實測把伺服器整個關掉（curl 拿到 HTTP 000）後重新載入，頁面完整開啟、
  VexFlow 從本機副本讀取、正常畫出樂譜。**

**過程中修掉的 bug**

- SW 原本一律 cache-first，結果是「你在電腦改完 push 上去，iPad 永遠開到舊版」。
  改成程式碼 network-first、大型靜態檔 cache-first。這個問題是在測試時被自己的 SW
  擋掉新 CSS 才發現的。
- 頂欄的「換一題」被撐成 1141px：`.btn-primary` 帶著側欄時代的 `width:100%`，
  且宣告在本檔後段，同分時勝出。改用 `.topbar .tb-btn` 提高 specificity。

### P3 附錄 — 原始規劃

1. **版面翻轉成「譜優先」**：
   - 譜佔滿畫面，設定收進側邊抽屜（預設關閉），頂部只留 BPM、開始、下一題
   - 橫向：兩行譜；直向：一行大譜表 + 下一段預覽
   - `env(safe-area-inset-*)` 處理瀏海與底部橫條
2. **觸控**：所有可點區域 ≥ 44px；`touch-action:manipulation` 關掉雙擊縮放；手勢——左滑下一題、右滑回顧、點譜面播放/暫停。鍵盤快捷鍵保留給桌機。
3. **iOS 音訊三個坑**：
   - AudioContext 必須在使用者手勢中建立/resume（現在大致 OK，但要加一個明確的「點我啟用聲音」首次互動）
   - **靜音鍵**：iOS 的 WebAudio 走鈴聲通道，實體靜音鍵一開節拍器就沒聲。Safari 16.4+ 有 `navigator.audioSession.type = 'playback'` 可以解；不支援時退回播放一段無聲 `<audio>` 迴圈搶佔播放通道，並在 UI 上明講。
   - **螢幕變暗 / timer 節流**：用 `navigator.wakeLock`（iOS 16.4+）在練習時保持螢幕常亮；節拍排程全部改用 AudioContext 硬體時鐘。
4. **PWA**：`manifest.json` + `apple-touch-icon` + Service Worker 快取 → 加到主畫面、全螢幕、離線可用。
5. **離線 VexFlow**：現在三個 CDN 全掛就只剩錯誤訊息。做兩個版本——
   - 部署版：SW 快取 VexFlow
   - 單檔版：VexFlow 內嵌進 HTML（約 +600KB），AirDrop 到 iPad 用 Files 開，完全不需要網路
6. **交付路徑**（見「待決定」）。

### F — 追加修正 ✅ 已完成

**F-1 節拍器音量／靜音**
音量滑桿（0–100）＋靜音勾選。靜音時 `_click` 直接不建立任何音訊節點，
但拍點條與進行中游標照常運作，所以看得到拍子。拉動音量會自動解除靜音。
不影響「播放解答音」——那條路走的是 `Audio` 的 master，兩者分開。

**F-2 段落建檔與回顧調閱**
連續流每推進一段就建檔，譜面下方一排存檔籤（編號 · 調 · 和聲 · 終止式），點了即可調閱。
- 只存 `usedCfg`（一段幾十 bytes），調閱時用同一份設定重新生成，
  實測 7 段各重複調閱三次全部完全一致，且與當初練的那一段逐音相符。
- 調閱屬於回顧不是練習，所以會停下節拍器，並顯示「回到練習」。
- **按「換一題」或改任何出題設定 = 換新題目，存檔清空。** 這是使用者指定的行為。

過程中修掉的 bug：`openReview` 原本先畫譜再收起另一列，於是第一次是在
「兩列都在」的容器寬度下排版、第二次是在「已收起一列」的寬度下排版，
同一段會排出不同的音符間距。改成先收版面再畫。和弦模式有同樣問題，一併修掉。

### P4 — 回顧樂譜（跨場次的完整版）✅ 已完成

`js/library.js`。刻意與 F-2 的「本次練習存檔」分開，因為兩者的清空規則相反：
本次存檔按換一題就清空（使用者指定），長期紀錄關掉瀏覽器再開還在。

- **只存 cfg**，要看譜時用同一份設定重新生成。實測每筆 359 bytes，上限 2000 筆約 0.7 MB。
  以 cfg 的 FNV-1a 雜湊當身分證，同一段再練一次是累加次數而不是多一筆。
- **重畫不算練習**：縮放、開關抽屜都會重繪，但只有「換了一段新的」才記錄。實測驗證過。
- **標記**：譜下方的「✗ 這段沒彈好」或按 <kbd>X</kbd>，進複習清單，可跨場次調閱重練。
- **統計**：練過段數、累計分鐘（節拍器實際在跑的時間，關分頁也會結算）、
  待複習數、碰過幾個調。「最常卡住」看的是**標記率**而不是練習次數 ——
  練得多還是常出錯才是真的卡住；並限定練過 3 次以上才納入排名，
  否則練一次標一次的調會永遠排第一。另外會列出從沒碰過的調。
- **拼頁列印**：把複習清單（沒有標記時退回最近 8 段）排成一張練習單，
  固定 700px 寬繪製，不受目前視窗大小影響。Safari 不一定發 `afterprint`，補了逾時保險。
- localStorage 不可用時（iOS 無痕、`file://`）自動降級並在面板上說明，不會讓程式掛掉。

**實測**：跑 10 段 → 12 筆紀錄；縮放來回不灌水；標記／取消；跨場次調閱兩次逐 path 一致；
換一題後本次存檔 11→1 而長期紀錄保留；重新載入頁面後 17 筆、2 個標記、複習清單可用。

**過程中修掉的 bug**：`sw.js` 的快取清單漏了新加的 `js/library.js`。
離線時那個模組會載不到，而且**只有真的斷網才會發現**。
補了 `tools/audit-sw.mjs` 自動比對快取清單與實際檔案，之後新增模組不會再漏。

### P4 附錄 — 原始規劃

- **歷史紀錄**：每產生一段就存 `{seed, cfg, 時間, 調, 難度}` 到 localStorage（只存種子，一段幾十 bytes，可以存幾千段）。
- **回顧抽屜**：時間軸列出過去的段落，點一下重新畫出來、重播、重練。
- **標記**：練習中隨時按「這段沒彈好」→ 進入複習清單；複習清單可以單獨循環播放。
- **拼頁列印**：勾選 N 段 → 排成一張 A4 練習單列印或存 PDF（現有 `@media print` 已有基礎，擴充成多段拼版）。
- **統計**：練了幾分鐘、各調各難度分佈、哪個調最常被標記「沒彈好」→ 直接推薦下一次練什麼。

### P5 — 和弦模式大改 ✅ 已完成

| | 改版前 | 改版後 |
|---|---|---|
| 和弦類型 | 6 | **29** |
| 進行 | 5 | **28**（分五類） |
| Voicing | 4 | **9**（其中 2 種是雙手） |
| Comping 節奏 | 無（一小節一顆全音符） | **7 種 + 走路低音** |
| 譜面 | 單行低音譜號 | **可大譜表** |
| 播放 | 平均八分 | **可調 swing** |

- `js/core/chords.js` 29 種：9/11/13/sus4/alt/♭9/♯9/♯11/♭13/♯5/6-9/maj7♯11/m9/m11/m(maj7) 等。
  音程用「級數＋半音」表示，所以 D♭7♯11 的 ♯11 拼成 G 而不是 F𝄪。
- `js/gen/voicing.js` 9 種：shell、guide、rootless A/B、**drop 2**、**drop 3**、
  **四度堆疊**（So What）、**上部結構**（左手 shell + 右手三和弦，雙手）、
  **左手根音 + 右手和弦**（雙手）。上部結構的右手三和弦依屬和弦的變化音決定 ——
  右手按的是好按的三和弦，聽起來卻是完整的變化屬和弦。
- `js/gen/comping.js`：whole / half / **Charleston** / 反拍切分 / 搶拍 / 每拍一下 / 前一拍先進，
  載入時驗證時值。**走路低音**每拍一音，第四拍用半音或級進接近下一個和弦的根音。
- 進行 28 條：基礎 7、代理 6（三全音代理、backdoor、副屬連鎖、經過減和弦、小下屬、sus 解決）、
  藍調 4（基本／Jazz／Bird／小調）、曲式 9（Rhythm Changes A・B、Coltrane、Autumn Leaves、
  Blue Bossa、Satin Doll、A Train、All the Things、Fly Me）、調式 2。
  全部以「距主音的音程」描述，所以每一條都能移到十二個調。
- swing 實測：♩=60 時後半拍由 0.5s 移到 0.62s / 0.7s，正拍完全不動。
- **藏答案時仍然可以播放** —— 和弦符號照畫、音符不畫，但播放計畫照樣建好（實測 32 個事件）。

**實測**：28 進行 × 9 voicing × 4 comping = 1008 組 × 12 調，零空白、零錯誤、
strict voice 零降級，其中 420 組是雙手。

**過程中修掉的 bug**：新增的 `voicing.js` 與 `comping.js` 又漏進 `sw.js` 的快取清單 ——
上一輪補的 `tools/audit-sw.mjs` 這次直接抓到，沒有等到斷網才發現。

### P5 附錄 — 原始規劃

單調的真正解法有三層，不只是「多加幾個進行」。

**1. 和弦字典擴充**（現在 6 種 → 約 25 種）
`maj7 / 6 / 6-9 / maj7♯11 / m7 / m6 / m9 / m11 / m(maj7) / 7 / 9 / 13 / 7sus4 / 7♭9 / 7♯9 / 7♯11 / 7♭13 / 7alt / 7♯5 / dim7 / m7♭5 / aug`

**2. 進行庫分類**（現在 5 種 → 約 30 種）
- 基礎：ii–V–I（大/小）、I–vi–ii–V、iii–vi–ii–V、I–IV–iii–vi
- 代理與經過：三全音代理 ii–♭II7–I、backdoor ♭VII7–I、♭III7、副屬連鎖 III7–VI7–II7–V7、經過減和弦 I–♯i°–ii
- 藍調：基本 12 小節、Jazz Blues（塞滿 ii–V）、Bird Blues、小調藍調
- 曲式：Rhythm Changes A 段 / B 段、Coltrane Changes、Autumn Leaves 全曲、Blue Bossa、Take the A Train、Satin Doll、All the Things You Are 前八、Fly Me to the Moon
- 調式：So What / Impressions 這類 modal vamp、四度和聲

**3. 這一項才是關鍵 —— 讓和弦模式也變成雙手練習**
- **Voicing 擴充**：shell / guide tone / rootless A・B / **drop 2** / **drop 3** / **四度堆疊** / **上部結構三和弦（左手 shell + 右手 upper structure）**
- **Comping 節奏**：不再是一小節一顆全音符。加 Charleston、前八後二、切分 comping、兩拍一換
- **走路低音**：左手 walking bass 四分音符 + 右手 voicing —— 這是爵士鋼琴最實用的雙手練習
- **導音連接**：切換和弦時自動選最省力的轉位，讓 3–7 半音移動（現在 `chordShellForm` 只有 137/173 兩種交替，太粗）
- **Swing 播放**：八分音符改 swing 比例（可調 50–67%），現在是死板的平均八分
- **12 調輪替 + 進度追蹤**：把「12 個調全部過一次」做成一個可勾選的關卡，接上 P4 的統計

---

## 三、建議順序與規模

| 階段 | 內容 | 相對規模 | 完成後你能做什麼 |
|---|---|---|---|
| P0 | 種子亂數 + 全調性核心 + 節奏驗證 | 中 | 所有調都能出題，題目可重現 |
| P1 | 雙手模式庫 + 手部設定 | 中 | 真正的雙手視譜 |
| P2 | 四小節串流 + 預讀 + 游標 | 中大 | 開節拍器就能無限視譜 |
| P3 | iPad 版面 + PWA + 離線 | 中 | **在 iPad 上真的開始用** |
| P4 | 回顧樂譜 + 標記 + 列印 | 中 | 回頭練沒彈好的段落 |
| P5 | 和弦字典 + 進行庫 + comping + walking bass | 大 | 和弦模式不再單調 |

P3 排在中間，是因為它決定你什麼時候能真的開始用；P5 最大，放最後單獨做。

---

## 四、待決定

1. **iPad 怎麼拿到這個檔案**
   - (A) GitHub Pages + PWA — 免費、可加到主畫面、離線、以後改完 push 就更新。**建議**
   - (B) 單檔內嵌 VexFlow，AirDrop / iCloud 傳過去用 Files 開 — 完全離線但沒有主畫面圖示，且 iOS 對 `file://` 的 localStorage 不保證可用（回顧功能可能存不住）
   - (C) 兩個都產（部署版 + 離線單檔備份）
2. **要不要拆檔**：現在全部塞在一個 HTML。P5 之後大概會到三、四千行。可以維持單檔（好傳、好備份），或拆成 `index.html` + `js/` 多檔（好維護，但就必須走 (A) 部署）。
3. **先做哪一段**：照上表順序，或先跳 P3 讓 iPad 先能用。
